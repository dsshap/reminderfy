%h2 Sign Up
= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), html: { class: 'form-horizontal no-label-form'}) do |f|
  = f.error_notification
  .form-inputs
    = f.input :establishment_name, :required => true, :autofocus => true, label: false, placeholder: "Establishment Name"
    = f.input :email, :required => true, label: false, placeholder: "Email"
    = f.input :fname, :required => true, label: false, placeholder: "First Name"
    = f.input :lname, :required => true, label: false, placeholder: "Last Name"
    = f.input :phone_number, :required => true, label: false, placeholder: "Phone Number"
    = f.input :password, :required => true, label: false, placeholder: "Password"
    = f.input :password_confirmation, :required => true, label: false, placeholder: "Password Confirmation"
    = f.button :submit, "Sign up", class: 'btn btn-large btn-primary'
= render "devise/shared/links"